# 06 — Numerical Methods (C)

Revisiting: root finding, interpolation, numerical integration, iterative solvers.

**Tools:** `gcc`

## Setup

```bash
gcc src/root_finding.c -o root_finding -lm
./root_finding
```

## Projects

- [ ] `src/root_finding.c` — bisection, Newton-Raphson, and secant method side by side, compare convergence speed
- [ ] `src/interpolation.c` — linear and Lagrange interpolation on noisy sample points
- [ ] `src/integration.c` — trapezoidal rule and Simpson's rule, compare error vs. step size

## Log

_What clicked seeing this in code vs. on paper — fill in as you go._
